# Changelog

## [Unreleased]

### Fixed
- Replaced dependency on react-native-country-picker-modal with a version that removes defaultProps warnings
- Fixed React warning: "Warning: defaultProps is deprecated and will be removed in a future major release. Use JavaScript default parameters instead."
- All functionality remains the same with no API changes
